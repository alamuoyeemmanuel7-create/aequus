[build]
command = "pnpm install --frozen-lockfile && pnpm build"
functions = "apps/web/.next/server"
publish = "apps/web/.next/static"

[build.environment]
NODE_VERSION = "20"
PNPM_VERSION = "9"

[[redirects]]
from = "/*"
to = "/.next/server/:splat"
status = 200
